## 使用方法

1.

```html
<RangeForm :form="form" :fieldNameList="['opt', 'value', 'rangeMin', 'rangeMax']"></RangeForm>
```

2. 参数

- form 绑定的表单
- fieldNameList 自定义表单参数的名字 默认为['opt', 'value', 'rangeMinN', 'rangeMax']
- unit 单位显示
